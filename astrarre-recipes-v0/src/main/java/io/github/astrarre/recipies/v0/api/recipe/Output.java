package io.github.astrarre.recipies.v0.api.recipe;

public interface Output {
	boolean accept(Object...params);

	interface Mono<A> extends Output {
		boolean accept(A val);
	}

	interface Mono<A> extends Output {
		boolean accept(A val);
	}

	interface Mono<A> extends Output {
		boolean accept(A val);
	}

	interface Mono<A> extends Output {
		boolean accept(A val);
	}

	interface Mono<A> extends Output {
		boolean accept(A val);
	}
}
